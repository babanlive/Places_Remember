version: '3.9'

services:
  db:
    restart: always
    container_name: postgres
    image: postgis/postgis:latest
    env_file:
      - config/.env
    volumes:
      - /postgres_data:/var/lib/postgresql/data
    networks:
      - db

  web:
    build:
      target: development
      context: .
      dockerfile: ./Dockerfile
    image: on1x/app_development
    container_name: app_django
    depends_on:
      - db
    volumes:
      - .:/app
      - static_volume:/app/staticfiles
      - media_volume:/app/media
    env_file:
      - config/.env
    networks:
      - db

    
volumes:
  postgres_data:
  static_volume:
  media_volume:

networks:
  db:
  